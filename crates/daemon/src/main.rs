fn main() {
    // Placeholder — populated by Plan 4. The real daemon must NOT write
    // anything to stdout except NDJSON JSON-RPC responses; use stderr for
    // diagnostics.
    eprintln!("nomai-daemon skeleton (Plan 1 placeholder)");
}
